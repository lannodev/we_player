import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:we_player/app/modules/album/album_page.dart';

void main() {
  testWidgets('AlbumPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AlbumPage(title: 'Album')));
    //  final titleFinder = find.text('Album');
    //  expect(titleFinder, findsOneWidget);
  });
}
